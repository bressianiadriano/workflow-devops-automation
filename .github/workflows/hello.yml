name: Hello Github

on: 
  push:
      branches: 
          - main 

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: exibir mensagem no log
        run: echo "git action configurado"
